# 2026-09-03c — stage split and per-slot usage of `GlobalConstants` (static, home PC)

Output of the new toolkit tool over the shipped bundle — interface metadata only (no shader
bytecode is stored here):

```
python flat-to-vr-RE-toolkit/tools/dxbc-usage.py "Mad Max/Shaders_F.shader_bundle" GlobalConstants --slots 0-5,9,16-19
```

`2026-09-03c-globalconstants-stage-and-slot-usage.txt` — sections:

- **A** — the 512-byte layout is declared by 186 **vertex** shaders and the 2352-byte layout by
  465 **pixel** shaders; nothing declares 3136.
- **B** — per (size, stage), how many shaders read each 16-byte slot. Vertex side: slots 18 and
  19 are read by none; 0..3 by 15; 9 by 146.
- **C** — sample instructions for slots 0..5, 9 and 16..19. Slots 0..3 are a `mul/mad/mad/add`
  chain on the input position (straight into `SV_Position` in shaders 0009 and 0023); 16/17 are
  `xyz offset + w scale`; 9 is subtracted from world positions (camera position).
- **D** — for vertex shaders, the cbuffer slots on the register chain feeding `SV_Position`:
  `InstanceConsts` b1 slots 0..3 in 109 shaders, `cbInstanceConsts` b1 0..3 in ~35,
  `GlobalConstants` b0 0..3 in 15. (The walk is 4 hops deep and over-approximates: fog/fade
  parameters that feed the same temporaries show up too.)

Read alongside `modding-notes/2026-09-03c-the-two-layouts-are-vertex-and-pixel-and-the-camera-matrix-is-per-pass.md`.
Disassembly cache lives in the session scratchpad and is regenerated by the tool (needs `fxc`).
